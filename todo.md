# Development Tracker 🚀

## 🐛 Known Issues / Bugs
- [ ] **TypeBox Stickiness**: The chat input box (TypeBox) pops up but does not return to its initial position after use.
- [/] **UI Lag**: The UI lags significantly while the AI is streaming a response.
- [x] **Improve markdown rendering** : In the chat the markdown rendering different text styles and formats is not consistent and hhas some issues while wrapping text and rendering lists and code blocks. Also the table redering creats extra blocks and does not render properly.
- [/] **Chat ui scrolls when opening the sidebar**: When the sidebar is opened the chat scrolls to the top instead of staying in place. some time if ai replying and sidebar is opened the chat some tiem expand redome thinking ui, 
- [x] **chat should stay on bottom** : When user open the app for first time the chat should stay on bottom and show the latest messages instead of showing the top of the chat.
- [x] **regenerate button scroolling**: When the regenerate button is pressed the chat scrolls to the top instead of staying in place or going to the message being regenerated.
- [ ] **context info** : add the ui to show used and left context tokens and other request details.
## 💡 Upcoming Features & Improvements
- [ ] **Custom Reasoning Parameters**: Add support for `reasoning_effort` (high/medium/low) for models supporting it (e.g., o1/o3).
- [ ] **Other Parameters**: Add support for `top_p`, `frequency_penalty`, etc., in the Mode or Provider settings.
- [/] Search Refinement (Partially Done): AI Search and AI Tab rename (as mentioned in Project Status).
- [ ] **api request details with context**: instead of showing the typing cursor in ui while making the request show the request details like api request reponse

---
*Add your notes, bugs, and feature ideas here.*
